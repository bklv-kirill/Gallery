1. TERMINAL -> docker compose up -d nginx
2. TERMINAL -> docker compose run --rm -it composer install
3. TERMINAL -> docker exec -it php-container php artisan migrate --seed
3. TERMINAL -> docker exec -it php-container php artisan storage:link

4. MYSQL:
   - user: root
   - password: root
   - port: 3306
   - env: ./mysql.env

5. PHPMYADMIN:
   - localhost:8080
   - env: ./phpmyadmin.env